package com.smartsolar.stations.fieldops.data

/** STUB — Member 4 owns QR verify/finalize (POST /api/qr/verify, /api/qr/finalize). */
object QrService {
    const val FUTURE_VERIFY = "POST /api/qr/verify"
    const val FUTURE_FINALIZE = "POST /api/qr/finalize"
}
